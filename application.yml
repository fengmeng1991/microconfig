spring:
  profiles:
    active:
      - dev
---
spring:
  profiles: dev
  application:
    name: mocroconfig-test-dev

---
spring:
  profiles: default
  application:
    name: microconfig-test-default
